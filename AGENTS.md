# Grok Build Instructions

For the current frontend UI implementation task:

1. Before modifying any project file, read `GROK_UI_BUILD.md` completely.
2. Read the root-level `DESIGN.md` completely.
3. Inspect the existing repository, including `package.json` and `src/`.
4. Treat `GROK_UI_BUILD.md` as the authoritative task specification.
5. Treat `DESIGN.md` as the authoritative design-system reference, subject to the explicit overrides in `GROK_UI_BUILD.md`.
6. Follow all scope boundaries, non-goals, acceptance criteria, and verification requirements in `GROK_UI_BUILD.md`.
7. Do not implement WebMCP, backend, authentication, deployment, or unrelated features.
8. Do not expand scope without explicit user instruction.
9. Implement the task directly; do not merely propose a design or return a tutorial.
10. Verify the implementation according to `GROK_UI_BUILD.md` before reporting completion.